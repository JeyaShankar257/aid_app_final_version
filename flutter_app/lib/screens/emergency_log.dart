import 'package:flutter/material.dart';

class EmergencyLogScreen extends StatelessWidget {
  const EmergencyLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Emergency Log')),
      body: Center(child: Text('Emergency Log Feature')),
    );
  }
}
