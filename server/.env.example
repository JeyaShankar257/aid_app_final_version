# Example environment variables for the server
# Copy this file to .env (locally only) and fill the values.

# SendGrid API key (use a restricted key with Mail Send permission)
#SENDGRID_API_KEY=your_sendgrid_api_key_here

# From address to use when sending emails (must be a verified sender in SendGrid)
#SENDER_EMAIL=your_verified_sendgrid_sender@example.com

# --- Brevo SMTP settings ---
# SMTP host for Brevo (default: smtp-relay.brevo.com)
BREVO_SMTP_HOST=smtp-relay.brevo.com
# SMTP port for Brevo (default: 587)
BREVO_SMTP_PORT=587
# Brevo SMTP user (your Brevo SMTP login)
#BREVO_SMTP_USER=your_brevo_smtp_user
# Brevo SMTP password (your Brevo SMTP API key)
#BREVO_SMTP_PASS=your_brevo_smtp_smtp_key
# Sender email (must be a verified sender in Brevo)
#BREVO_SENDER_EMAIL=your_verified_sender@example.com


# Optional: server port override (defaults to 5000 if unset)
PORT=5000

# Optional: NODE environment
NODE_ENV=production

# Optional: if you later add Redis for token store
# REDIS_URL=redis://:password@host:port/0
